# 피처 생성 파이프라인 설계

## 1) 목표
- 과거 회차까지의 정보만 사용해 다음 회차 번호를 예측할 수 있는 학습용 피처를 생성한다.
- 데이터 누수(leakage)를 방지하기 위해 목표 회차(draw_no)의 데이터는 피처 계산에 사용하지 않는다.

## 2) 입력 데이터
- 소스: `data/processed/lotto_draws.csv`
- 필드: `draw_no`, `draw_date`, `n1..n6`, `bonus`, (금액 관련 컬럼)

## 3) 출력 데이터 (권장 스키마)
- 형태: 번호 단위(number-level) 데이터셋
- 행 개수: `(유효 회차 수) * 45`
- 주요 컬럼:
  - `draw_no`: 예측 대상 회차 번호
  - `number`: 후보 번호 (1~45)
  - `label`: 해당 회차에 실제로 등장했으면 1, 아니면 0
  - 피처 컬럼들(아래 4절 참고)
- 파일: `data/features/lotto_features.parquet` 또는 `data/features/lotto_features.csv`
- 메타데이터: `data/features/feature_config.json` (윈도우/감쇠 등 설정 저장)

## 4) 피처 설계 (권장)

### A. 빈도 기반 (Frequency)
- `freq_5`, `freq_10`, `freq_20`, `freq_50`, `freq_100`
  - 최근 N회차 중 해당 번호 등장 횟수
- `freq_rate_5`, `freq_rate_10`, ... 
  - `freq_N / N` 비율

### B. 최근성/간격 (Recency)
- `last_seen_gap`
  - 직전 등장 이후 지난 회차 수
- `last_seen_draw_no`
  - 마지막으로 등장한 회차 번호 (없으면 0)
- `miss_streak`
  - 연속 미등장 회차 수(= `last_seen_gap`)

### C. 추세/변화 (Trend)
- `trend_10_50`
  - `freq_10 - (freq_50 * 10 / 50)`처럼 단기-장기 차이
- `trend_20_100`
  - `freq_20 - (freq_100 * 20 / 100)`

### D. 지수 가중 빈도 (Exponential Decay)
- `ewm_0_9`, `ewm_0_95` 등
  - 회차 간 감쇠율을 적용한 누적 빈도

### E. 공출현(선택)
- `cooc_prev_20`, `cooc_prev_50`
  - 최근 N회차에서 “이 번호가 직전 회차 번호들과 함께 등장한 횟수”
  - 현재 구현은 직전 회차 번호 집합과의 공출현 횟수를 계산

### F. 번호 자체 특징
- `number`
  - 번호 자체를 입력으로 포함 (1~45)
- `number_bucket`
  - 1~15 / 16~30 / 31~45 구간 버킷

### G. 날짜 기반(선택)
- `draw_month`, `draw_weekday`
  - 달/요일 파생 피처

## 5) 생성 로직(시간 순, 누수 방지)
- 회차를 `draw_no` 오름차순 정렬
- 각 회차 `t`에 대해:
  - 히스토리 = `1..t-1` 회차만 사용
  - 후보 번호 1~45에 대해 피처 계산
  - 실제 `t`회차 번호에 포함되면 `label=1`
- `max_window`(예: 100) 이후부터 학습용 데이터 생성
  - 예: `max_window=100`이면 `t >= 101`부터 생성

## 6) 분할 전략
- 시간 순 분할만 사용
- 예: 마지막 100회차를 테스트, 그 이전은 학습

## 7) 모델 입력 형태
- 번호 단위 분류/랭킹 모델
  - 예측 확률이 높은 상위 6개를 추천 번호로 사용

## 8) 초기 구현 우선순위
1. 빈도 + 최근성 + 추세 + 지수 가중 빈도 (A+B+C+D)
2. 번호/날짜 기반 피처 (F+G)
3. 공출현 피처 (E)
