# PRD: 로또 당첨 번호 예측 ML 프로젝트

## 1) 개요
- 목적: 과거 로또 당첨 번호 데이터를 기반으로 학습하여, 다음 회차의 번호 조합을 예측/추천하는 모델과 파이프라인을 구축한다.
- 핵심 산출물: 데이터 수집/정제 파이프라인, 모델 학습/평가 코드, 예측 결과 리포트 및 간단한 사용 인터페이스(출력/시각화).

## 2) 배경 및 문제 정의
- 배경: 사용자들은 “과거 데이터로 다음 번호를 더 합리적으로 선택할 수 있는가”에 관심이 있다.
- 문제 정의: 과거 당첨 번호 시계열을 활용해 다음 회차의 번호 조합(6개)을 예측/추천하는 모델을 만든다.
- 전제: 로또는 본질적으로 무작위성이 높아 예측 정확도에는 한계가 있다. 따라서 “확률적 추천”과 “분석/실험 플랫폼” 관점으로 접근한다.

## 3) 목표 (Goals)
- G1. 신뢰 가능한 데이터 파이프라인 구축(자동 수집/정제/검증).
- G2. 최소 1개 이상의 베이스라인 모델과 1개 이상의 개선 모델 구축.
- G3. 재현 가능한 실험과 성능 평가 리포트 제공.
- G4. 예측 결과를 사용자에게 명확히 전달(출력 포맷/시각화).

## 4) 비목표 (Non-Goals)
- N1. “당첨 보장” 또는 확정적 예측 제공.
- N2. 복잡한 웹/모바일 서비스 운영(초기 단계에서는 제외).
- N3. 외부 유료 데이터 연동.

## 5) 사용자 / 페르소나
- P1. 데이터/ML 실험에 관심 있는 개발자 또는 분석가
- P2. 로또 번호 선택을 재미로 개선해보고 싶은 일반 사용자

## 6) 성공 지표 (Success Metrics)
- S1. 데이터 최신성: 최근 회차 업데이트 지연 7일 이내
- S2. 재현성: 동일 설정에서 동일 결과 재생산 가능
- S3. 모델 성능: 베이스라인 대비 평균 적중 개수/Top-K 적중률 개선
- S4. 사용성: 예측 결과를 1분 이내에 확인 가능(노트북/콘솔)

## 7) 기능 요구사항 (Functional Requirements)
- FR1. 데이터 수집
  - 공식/신뢰 가능한 소스에서 당첨 번호 수집
  - 회차, 날짜, 당첨 번호(6개), 보너스 번호 저장
- FR2. 데이터 정제/검증
  - 결측/중복/오류 데이터 탐지 및 정리
  - 번호 범위(1~45) 및 중복 없는 조합 검증
- FR3. 데이터셋 구성
  - 학습/검증/테스트 분리(시간 순 분할)
  - 피처 생성(빈도, 간격, 조합 통계, 시계열 특징 등)
- FR4. 모델 학습
  - 베이스라인(예: 빈도 기반, Markov-like, 단순 통계)
  - 개선 모델(예: Gradient Boosting, RNN/Transformer 등)
- FR5. 평가 및 리포트
  - 평균 적중 개수/Top-K 적중률/랜덤 대비 개선율 정의
  - 실험 로그 및 결과 저장(노트북/콘솔 요약)
- FR6. 예측 결과 출력
  - 다음 회차 추천 번호 K세트 출력
  - 노트북 리포트와 콘솔 요약으로 결과 제공

## 8) 비기능 요구사항 (Non-Functional Requirements)
- NFR1. 재현성: 랜덤 시드/환경 고정
- NFR2. 유지보수성: 모듈화된 코드 구조
- NFR3. 확장성: 새로운 모델/피처 추가 용이
- NFR4. 성능: 로컬 환경에서 학습/예측 5분 내 완료(초기 목표)

## 9) 데이터 요구사항
- 데이터 소스: 동행복권 공식 당첨 번호 공개 데이터
- 최소 데이터 범위: 전체 회차(가능하면 전부)
- 저장 포맷: CSV(원본), Parquet(가공본)

## 10) 모델링 방향
- 베이스라인:
  - 단순 빈도 기반 추천
  - 최근 N회차 가중치
- 개선 모델:
  - 피처 기반 분류/랭킹 모델
  - 시계열 모델
- 출력:
  - 확률/점수 기반 Top-K 조합

## 11) 평가 전략
- 시간 순 테스트(미래 회차를 테스트로 사용)
- 지표 예시:
  - 평균 적중 개수
  - Top-K 적중률(6개 중 n개 이상 맞춤)
  - 랜덤 대비 개선율

## 12) 리스크 및 대응
- R1. 본질적 무작위성 → 기대치 관리(리포트에 명시)
- R2. 과적합 → 엄격한 시간 순 검증
- R3. 데이터 품질 문제 → 자동 검증 로직

## 13) 윤리/고지
- “예측 결과는 확률적 추천이며 당첨을 보장하지 않음” 명시
- 사용자의 과몰입 방지를 위한 고지 문구 포함

## 14) 마일스톤 (초기 제안)
- M1. 데이터 수집/정제 파이프라인 구축 (1주)
- M2. 베이스라인 모델 + 평가 지표 구현 (1주)
- M3. 개선 모델 1종 + 리포트 자동화 (1~2주)
- M4. 예측 결과 출력/시각화 (1주)

## 15) 오픈 이슈 / 결정 필요
- 추가적인 랭킹 지표(MRR/Hit@K) 도입 여부
